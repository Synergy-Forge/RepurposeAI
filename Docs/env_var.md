# following changes made on Vercel

- Configured the `NEXTAUTH_URL` environment variable with the application's URL.
- Generated the `NEXTAUTH_SECRET` using Node.js and set it as an environment variable for secure session encryption.

These changes ensure that NextAuth.js works correctly in production on Vercel.
